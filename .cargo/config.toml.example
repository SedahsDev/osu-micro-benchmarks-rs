# Prefer env vars over committed rpaths. Example local override:

[target.x86_64-unknown-linux-gnu]
rustflags = [
    "-C", "target-cpu=native",
    # "-L", "/path/to/ucc/lib",
    # "-L", "/path/to/ucx/lib",
    # "-L", "/path/to/pmix/lib",
    # "-C", "link-arg=-Wl,-rpath,/path/to/ucc/lib:/path/to/ucx/lib:/path/to/pmix/lib",
]
